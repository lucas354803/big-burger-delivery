const produtos=[{nome:'Big Burger',descricao:'Hambúrguer artesanal da casa',preco:23.90},{nome:'Combo Duplo',descricao:'2 burgers + fritas + refri 600ml',preco:39.90},{nome:'Combo Família',descricao:'4 burgers + 4 fritas + refri 1,5L',preco:84.90}];let carrinho=[];const fmt=v=>Number(v).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});function render(){menu.innerHTML=produtos.map((p,i)=>`<div class="card"><h3>${p.nome}</h3><p>${p.descricao}</p><div class="price">${fmt(p.preco)}</div><button class="btn" onclick="add(${i})">Adicionar</button></div>`).join('');cart.innerHTML=carrinho.length?carrinho.map(p=>`<div class="row"><span>1x ${p.nome}</span><b>${fmt(p.preco)}</b></div>`).join(''):'<p class="small">Carrinho vazio.</p>';total.textContent=fmt(carrinho.reduce((s,p)=>s+p.preco,0));}function add(i){carrinho.push(produtos[i]);render()}async function finalizar(){const valor=carrinho.reduce((s,p)=>s+p.preco,0);result.innerHTML='<p>Gerando Pix...</p>';try{const r=await fetch('/api/create-pix',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({cliente_nome:nome.value,cliente_telefone:telefone.value,endereco:endereco.value,observacao:obs.value,itens:carrinho,valor_total:valor})});const data=await r.json();if(!r.ok)throw new Error(data.error+' '+(data.detalhes?JSON.stringify(data.detalhes):''));if(data.pix?.qr_code_base64){result.innerHTML=`<div class="ok"><h3>Pix gerado!</h3><img class="qr" src="data:image/png;base64,${data.pix.qr_code_base64}"><textarea readonly>${data.pix.pix_copia_cola}</textarea><p class="small">Após pagar, o webhook libera a entrega automaticamente.</p></div>`}else{result.innerHTML=`<div class="ok"><h3>Pedido criado</h3><p>${data.mensagem}</p></div>`}}catch(e){result.innerHTML=`<div class="err"><b>Erro:</b> ${e.message}</div>`}}render();
