const produtos=[{id:'big',nome:'Big Burger',descricao:'Hambúrguer artesanal da casa',preco:23.90},{id:'duplo',nome:'Combo Duplo',descricao:'2 burgers + fritas + refri 600ml',preco:39.90},{id:'familia',nome:'Combo Família',descricao:'4 burgers + 4 fritas + refri 1,5L',preco:84.90}];
let carrinho=[]; const brl=v=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
function render(){produtosEl.innerHTML=produtos.map(p=>`<div class="produto"><h3>${p.nome}</h3><p>${p.descricao}</p><div class="preco">${brl(p.preco)}</div><button onclick="add('${p.id}')">Adicionar</button></div>`).join('');renderCart()}
function add(id){const p=produtos.find(x=>x.id===id);const item=carrinho.find(x=>x.id===id);if(item)item.qtd++;else carrinho.push({...p,qtd:1});renderCart()}
function renderCart(){carrinhoEl.innerHTML=carrinho.length?carrinho.map(i=>`<div class="linha"><span>${i.qtd}x ${i.nome}</span><b>${brl(i.preco*i.qtd)}</b></div>`).join(''):'<p>Nenhum item ainda.</p>';totalEl.textContent=brl(carrinho.reduce((s,i)=>s+i.preco*i.qtd,0))}
const produtosEl=document.getElementById('produtos'),carrinhoEl=document.getElementById('carrinho'),totalEl=document.getElementById('total'),resultado=document.getElementById('resultado');
document.getElementById('finalizar').onclick=async()=>{try{if(!carrinho.length)throw new Error('Adicione um produto.');const payload={nome:nome.value,telefone:telefone.value,endereco:endereco.value,observacao:observacao.value,itens:carrinho,valor_total:Number(carrinho.reduce((s,i)=>s+i.preco*i.qtd,0).toFixed(2))};resultado.innerHTML='<p>Gerando Pix...</p>';const r=await fetch('/api/create-pix',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const data=await r.json();if(!r.ok||!data.ok)throw new Error(data.error||'Erro ao gerar Pix');resultado.innerHTML=`<div class="pix"><h3>Pix gerado ✅</h3>${data.qr_code_base64?`<img src="data:image/png;base64,${data.qr_code_base64}">`:''}<p><b>Copia e cola:</b></p><textarea readonly>${data.pix_copia_cola||''}</textarea><p>Pedido: ${data.pedido_id}</p></div>`}catch(e){alert(e.message);resultado.innerHTML=''}};
let deferredPrompt;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e});installBtn.onclick=()=>deferredPrompt&&deferredPrompt.prompt();render();
